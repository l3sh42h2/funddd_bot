# Claude session handoff — 16.09.2026

Исполнитель: Claude. Адресат: Codex (и владелец). Это сводка за одну сессию — что сделано, что где лежит,
что дальше. Не разрешение на выкат чего-либо сверх уже установленного `571d86f`.

## Что сделано сегодня

1. **Ревью + фикс R0-пакета** (`591aaf0`, `571d86f`, уже в этой ветке): арифметика в
   `PATCHNOTES/m4-accounting-audit-20260915.md` не сходилась с собственными доказательствами пакета
   (93/148 заявлено, реально 101→103/156→158) — поправлено по факту прогона, не переписыванием на слово;
   `match=` в `test_recovered_open_peer_requires_intact_terminal_proof` сужен до точного сообщения на каждый
   из 8 типов повреждения; добавлен кейс `incomplete_payload` — прод-код (`operation_roots.py`) уже был
   fail-closed, багов не нашлось. Обе правки прошли независимую проверку (сам ревьюер пересчитывал числа,
   не пересказывал).
2. **Выкат `571d86f` на прод** — сделал сам (drain → backup БД → switch → readiness через
   `deploy/deploy.sh`), позиция DQA9Q сверена до и после (не изменилась). **Важно:** `schema_version` в БД
   автоматически мигрировал 4→5 в момент первого коннекта нового кода (не только при использовании
   generic-пути, как предполагалось раньше) — простой code-only откат на `0f9037c` через symlink, скорее
   всего, уже не сработает без восстановления БД из бэкапа
   (`/var/lib/funding-bot/backups/20260915T224833Z-571d86fd7455-a19b18bf6ce3-trade.db`). Стоит явно обсудить
   с Codex, ожидаемое ли это поведение тулинга отката.
3. **Приёмка миграции целиком** (не только М4) — свёл все `AC-01`..`AC-29` из
   `docs/MIGRATION_ACCEPTANCE.md` против кода `571d86f`: 20 закрыто, 3 частично (только на синтетике),
   6 открыто. Открытое — не только М4:
   - **AC-07** (M2/граница): `trade/engine.py:650,1673` на живом (не-generic) пути всё ещё формирует HTML
     через `Sender`, а не нейтральный DTO — на generic-пути уже починено.
   - **AC-15/16/17/19/20** (М4, основной объём) — реальный (не синтетический) replay истории сделок; см. п.4.
   - **AC-22/25/26** (М5) — обрыв SSH среди деплоя, параллельный деплой, откат после сделок, UI-only релиз —
     спроектированы в коде, но ни разу не прогнаны по-настоящему (только один благополучный cutover).
   - **Не про код**: `M2_RESULT.md`/`M3_RESULT.md` сами пишут, что независимый ревьюер для M2 не назначен, а
     это было условием до M5 — M5 уже случился дважды.
4. **Real-data replay AC-15** — ветка `claude/m4-real-data-replay` (от `571d86f`, коммиты `fd47e9a`+
   `f6b96d1`, **запушена, не смёржена**). Реальных сделок в истории проекта всего две (`DQA9Q` живая,
   `D9W8M` пустая ABORTED). Legacy-учёт совпал побайтово между кодом до координатора и текущим прод-кодом —
   независимо сверено и против того, что боевое ядро само писало в `deal_marks`/`funding_income`. **Не
   доказывает**: generic/scoped-путь (`operations`, `scoped_accounting`) ни разу не активировался на проде;
   cost basis не реализован нигде в коде вообще (ни legacy, ни generic) — сравнивать нечего. Независимая
   проверка нашла и я поправил: отчёт печатал кошелёк DQA9Q открытым текстом (убрано); JSON-фикстуры несут
   2 реальных BSC tx_hash (этого достаточно для деанонимизации через блок-эксплорер) — репозиторий сейчас
   публичный, владелец подтвердил, что это приемлемо.
5. **Теневой мост `execution_scope` → `scoped_accounting.ProvenScope`** — ветка
   `claude/m4-scoped-accounting-shadow` (от `571d86f`, коммит `41656c1`, **запушена, не смёржена**).
   Владелец поручил написать конвертер, но НЕ авторизовал делать scoped_accounting авторитетным сейчас
   (моя рекомендация: подождать cost basis + больше реальных данных). Ключевая находка: `engine.deal_fills()`
   /`accounting.sources()`/`is_bound()` (а через них cabinet/dashboard/tg) переключаются на scoped-путь
   автоматически, как только в `scoped_deal_accounts` появляется строка — а её пишет только
   `scoped_accounting.bind_deal()`. Поэтому реализация **никогда не вызывает `bind_deal()`** — вместо этого
   отдельная append-only таблица `shadow_scope_bindings`, которую не читает ни один прод-модуль. Независимая
   проверка сделала собственную мутационную атаку (вставила `bind_deal()` в теневой путь) — поймано не только
   двумя тестами автора, но и третьим, независимым, более старым тестом `test_m4_execution_scope.py`.
   `proof_kind` для provenance кроме `authenticated_legacy_orders` — явный отказ, не догадка (открытый вопрос
   владельцу в патчноуте ветки).

## Что дальше — предлагаемый план

**М4 (главный объём, чтобы закрыть AC-15/16/17/19/20 по-настоящему):**
- Реализовать cost basis (нигде не начато) — без этого AC-15 не закрывается полностью независимо от того,
  что происходит со scoped_accounting.
- Смёржить `claude/m4-real-data-replay` и `claude/m4-scoped-accounting-shadow` в эту ветку (ревью Codex).
- Накопить больше реальных сделок под теневым мостом, прежде чем рассматривать scoped_accounting как
  авторитетный источник; переключение — отдельное решение владельца, не автоматическое следствие мержа.
- AC-07: перевести живой (не-generic) EVM-путь с `Sender(html)` на нейтральный DTO, как уже сделано для
  generic-пути.

**М5 (можно параллельно, не блокирует деньги так же жёстко):**
- Реальные fault-injection прогоны AC-22/25/26 на Linux-стейджинге (не на проде): обрыв SSH посреди
  `install`, параллельный `deploy.sh` от двух исполнителей, откат кода после состоявшихся сделок, UI-only
  релиз с несовместимым IPC.
- Разобраться, действительно ли `schema_version` теперь необратимо 5 — если да, задокументировать это как
  ограничение процесса отката, не как баг.

**Не про код:**
- Назначить/подтвердить независимого ревьюера для M2 (владелец).
- Position-resize/SL: `docs/POSITION_RESIZE_SL.md` на `origin/codex/position-resize` называет исполнителем
  Codex и требует делать это ПОСЛЕ приёмки миграции. Сегодня владелец в другом разговоре попросил Claude
  начать эту работу — я остановил агента на середине (ничего не запушено), не разобравшись, сознательно ли
  это переназначение. Стоит уточнить у владельца явно, прежде чем кто-либо начинает resize/SL заново.
- Дальше за пределами М4/М5 (из отдельного gap-анализа целевого состояния — Hyperliquid/Gate/Aster/OKX DEX/
  Solana DEX/дашборд/Telegram/pluggable-биржи): включение/выключение связки уже в основном конфиг; новых
  Telegram-команд (resize/SL/toggle биржи) и объединения дашборда с реальными позициями пока нет — отдельная
  фаза после М4.

**Открыто/устарело в документах** (не блокер, но вводит в заблуждение): `MIGRATION_RESULT.md`/
`M5_DEPLOY_STATUS.md` описывают релиз `aa446f2` от 14.09 тоном «почти готово» — для сегодняшнего `571d86f`
отдельного итогового receipt/отчёта в репозитории нет. Ни одного документа «весь кандидат целиком одобрен»
не существует — только цепочка узких ревью по кускам (R0, accounting-audit, real-data replay, shadow bridge).
