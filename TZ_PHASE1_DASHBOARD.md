# ТЗ. Фарм фандинга Binance ↔ Aster, фаза 1: дашборд

Дата: 2026-09-10 (уточнено ответами владельца в тот же день). Основание: `HANDOFF_BASIS_FUNDING.md` (09.09),
решения владельца в чате 10.09, замеры с VPS ireland 10.09 (раздел 2).

## 0. Решения владельца (зафиксировано)

| # | Решение |
|---|---|
| 1 | Биржи: **Binance** и **Aster** (asterdex.com). Других на старте нет. |
| 2 | Живём на **VPS ireland** (`admin@34.65.234.12`, рядом с lphedge и polypin-f). |
| 3 | Первая функция бота — **дашборд**: все монеты, текущий спред по фандингу, курсовой спред, история фандинга за 4 / 24 / 48 ч. |
| 4 | Вход в сделку — **только по команде владельца**. Бот входит клипами по **$10–15** без удара по стакану; для декс-ноги дополнительно учитывает комиссию сети. Это фаза 2, здесь только зафиксировано. |
| 5 | После входа бот показывает позицию live: плюс/минус, тики фандинга. Фаза 3. |
| 6 | Проект называется **`funding_bot`**. |
| 7 | Длинная нога: **сначала спот Binance**; дальше смотрим дексы — Solana и EVM (Robinhood Chain, BSC, Base, Arbitrum). Слой длинной ноги проектируется сменным с первого дня. |
| 8 | История фандинга собирается **на 30 суток**, не только 48 ч — для порогов фазы 2. |
| 9 | Диск ireland чистит владелец сам. |
| 10 | **Все финансовые ограничения — размер позиции, дневной стоп, число открытых позиций, пороги входа — задаёт и меняет только владелец.** В ТЗ и коде для них есть поле, но нет придуманного значения: пустой параметр = запрет, а не дефолт. |

Параметры владельца (заполняются им, в коде живут в `runtime/owner.toml`, без них торговый контур не стартует):

| параметр | значение |
|---|---|
| капитал на одну сделку | — |
| число открытых позиций | — |
| дневной стоп | — |
| порог спреда для входа | — |
| порог спреда для выхода / переворота | — |

## 1. Цель фазы 1

Живая таблица по всем монетам, которые торгуются перпом и на Binance, и на Aster: кто кому платит, сколько это в годовых, сколько накапало за 4 / 24 / 48 ч на каждой бирже и в спреде, чего стоит войти (курсовой спред и ширина книг). Обновление раз в 10 с, история без дыр, переживает рестарт. Никаких ключей и никаких денег — только публичные данные.

Не цель фазы 1: вход, ведение позиции, учёт PnL.

## 2. Факты о площадках (проверено 10.09.2026 с ireland)

**API.** Aster — клон Binance-futures-API: те же пути и поля, `https://fapi.asterdex.com` (v1-пути работают, документирован v3), WebSocket `wss://fstream.asterdex.com`, потоки `!markPrice@arr` (3 с / 1 с; поля `r` фандинг, `T` время расчёта, `p` марк, `i` индекс) и `!bookTicker`. Лимит **2400 веса/мин**, заголовок `X-MBX-USED-WEIGHT-1M`, 429 → 418 (бан от 2 мин до 3 дней). Binance fapi: 2400/мин, spot 6000/мин. Оба отвечают из Ирландии за 0.3–0.4 с. Следствие: **один клиент на две биржи, различаются только базовый URL и бюджет веса.**

**Вселенная.**
- Aster: 592 перпов (571 TRADING, 580 в USDT). Интервалы фандинга: **8 ч — 289, 4 ч — 259, 1 ч — 189, 2 ч — 3**. Кап ±2 % у 697 из 740, у остальных уже (±0.3…0.75 %).
- Binance: 571 перпов TRADING (528 USDT). `fundingInfo` отдаёт 782 записи (4 ч — 466, 8 ч — 313, 1 ч — 3); если символа там нет — 8 ч. Спот USDT TRADING — 490.
- **Пересечение перп∩перп = 394 пары** (USDT). Из них у 253 есть спот на Binance, у 251 — и то, и другое. 165 монет есть только на Aster (в основном токенизированные акции AAPL/AMZN/…): хеджировать нечем, в таблицу не попадают.
- Спот Aster: 68 пар, треть — TEST*. Для длинной ноги не годится, в фазе 1 не используем.

**Ловушка одинаковых тикеров.** `MEMEUSDT`: разница мидов Aster/Binance **+1 194 462 %**, `SLPUSDT` +217 %. Это не одна и та же монета либо разный множитель. Правило 10.09: пара считается одной монетой, только если |mid_A/mid_B − 1| < 3 %; иначе строка помечается «не тот актив» и в спред не идёт. (С 12.09 по решению владельца — не по цене, а по составу индекса перпа и контрактам монет, см. §6.) Префиксы `1000X`/`kX` нормализуются как в lphedge `perps.norm_symbol_factor`.

**Книги.** Полуспред Aster: p50 **10.4 бп**, p90 21 бп; Binance: p50 2.0, p90 4.2. Разница мидов между биржами: p50 2.8 бп, p90 10.4 бп. Aster в 5 раз тоньше — стоимость круга там задаёт спред, а не комиссия. Клип $10–15 книгу не двигает (BTC: лучшие уровни 5.6 BTC / 0.05 BTC).

**Комиссии.** Aster USDT-перп VIP0: **мейкер 0 %, тейкер 0.04 %**, −5 % при оплате в ASTER (мейкер обнулён 02.02.2026). Binance futures VIP0 0.02/0.05 %, спот 0.1/0.1 % (наш фактический тариф уточнить по счёту, не по прайсу).

**Срез спреда фандинга 10.09 18:57 UTC** по 394 парам, в годовых: p50 0 %, p75 13 %, p90 36 %, p95 58 %, максимум 349 %. Пар с |спред| ≥ 20 % — 66, ≥ 50 % — 27, ≥ 100 % — 6. Крайности — Aster с отрицательным знаком (SLP −338 %, FLUX −114 %, AXL −101 %): шорты платят лонгам, то есть заработок — лонг Aster / шорт Binance, а не спот-вариант.

**История.** `fundingRate?symbol=` отдаёт до 1000 расчётов (Aster BTC — с 12.10.2025). Без символа — 1000 последних по всем монетам: Aster покрывает **2 ч**, Binance 6 ч. Значит, 48 ч собираются посимвольно (394×2 вызовов, вес 1, у Binance общий лимит 500 за 5 мин на IP — темп ≤ 1.5/с, ~5 мин), а дальше досбор одним вызовом на биржу раз в 15 мин.

**Время расчёта** у обеих на круглых часах UTC, но интервалы у одной пары могут различаться (`1000CATUSDT`: Aster 1 ч, Binance 4 ч). Сравнивать ставки можно только приведёнными к часу.

**Интервал Aster меняется на ходу** (найдено при первом запуске 10.09 19:30): `fundingInfo` объявляет AWE как 4-часовой, а история показывает ежечасные расчёты до 14:00 и затем 16:00. Объявленный интервал годится для приведения ПРЕДСКАЗАННОЙ ставки к часу, но не как сетка истории: «ожидаемого N расчётов в окне» не существует. Полнота проверяется по слову биржи — предыдущий расчёт = `nextFundingTime − интервал`; дыра внутри окна — разрыв больше max(8 ч, 2×интервал) + льгота.

**VPS ireland**: 2 vCPU, 3.8 ГБ RAM (занято 0.7), Python 3.11.2, **диск 77 %, свободно 4.3 ГБ** — удержание снимков ограничено (§4).

## 3. Архитектура

Отдельный проект `funding_bot/` (`/Users/admin/Documents/vps/funding_bot`, на VPS `~/hyper/funding_bot`). Python 3.11, свой venv, один SQLite (WAL) `runtime/funding_bot.db`. Из lphedge копируются образцы: `http.py` (ретраи, 429), `lock.py` (`money_lock`, понадобится с фазы 2), `serve.py` (localhost + ssh-туннель), `deploy/deploy.sh` (ворота тестов → файлы → рестарт). Ничего из lphedge не импортируется — проекты не связаны кодом.

```
collector  (демон)      тик 10 с: premiumIndex ×2, bookTicker ×2 + spot bookTicker → память + снимок раз в 5 мин
                        раз в 15 мин: fundingRate без символа ×2 → funding_events (дедуп по PK)
                        раз в час: exchangeInfo ×2 + spot exchangeInfo → instruments, пересборка pairs
                        на старте: бэкфилл 30 сут посимвольно, идемпотентно (в фоне; таблица живёт с первого тика)
web        (демон)      GET / — таблица, всегда из живого состояния коллектора; GET /status — json
```

Клиент биржи — один класс `BinanceLike(base_url, weight_budget)`; Aster и Binance — два экземпляра. Бюджет веса читается из заголовка ответа, при использовании > 70 % тик пропускается и пишется в журнал (не 429 и тем более не 418).

Знак фандинга везде как у бирж: **плюс — лонги платят шортам.**

## 4. Данные

| таблица | ключ | содержание |
|---|---|---|
| `instruments` | (exchange, symbol) | статус, базовый актив, множитель (1000X), интервал фандинга (ч), кап/флор, есть ли спот на Binance, шаг цены/количества, minNotional |
| `pairs` | base | символ на Aster, символ на Binance, спот-символ Binance, флаг «не тот актив» с последним отношением мидов, время последней проверки. Поле спота — первый адаптер длинной ноги; дексы Solana/EVM добавятся сюда же отдельными колонками, не переделкой схемы |
| `funding_events` | (exchange, symbol, funding_time) | ставка за интервал, интервал (ч) на момент расчёта. Только вставка, никогда не обновляется |
| `snapshots` | (ts, base) | раз в 5 мин: ставка предсказанная A/B (в час), марк A/B, mid A/B, полуспреды, спот mid, спред фандинга (в час) |
| `health` | exchange | время последнего удачного тика, вес за минуту, число 429, возраст истории |

Удержание: `funding_events` — бессрочно (~8 тыс. строк/сут), `snapshots` — 30 сут (~10 МБ/сут), чистка ночью с чекпойнтом WAL внутри чистки (урок polypin3). Порог диска: при < 2 ГБ свободного коллектор перестаёт писать снимки и поднимает флаг в `health`, история фандинга пишется всегда.

Обязательное свойство истории: **полнота проверяется**. Для каждой пары и окна число расчётов сравнивается с ожидаемым по интервалу; при недостаче строка получает пометку «история неполная n/N», сумма не выдаётся за истину (долг lphedge с флагом `truncated` без потребителя здесь закрыт сразу).

## 5. Расчёты

- Ставка в час: `r_h = rate / interval_h`. Годовые: `APR = r_h · 24 · 365`. Показывать обе формы, сортировать по часовой.
- **Текущий спред** = `r_h(Aster) − r_h(Binance)` по предсказанным ставкам (`lastFundingRate` из `premiumIndex` — это ставка ближайшего расчёта, она плывёт до самого расчёта). Знак спреда говорит, где шорт: спред > 0 → шорт Aster / лонг Binance, спред < 0 → наоборот.
- **Спот-вариант** (лонг спот Binance + шорт перп Aster) доступен, только если есть спот на Binance; его доход = `r_h(Aster)` целиком, без вычета Binance-ставки. Показывается отдельной колонкой.
- **История 4 / 24 / 48 ч** — сумма фактически расчитанных ставок в окне по каждой бирже и по спреду (сумма A − сумма B), рядом фактическое число расчётов (не n/N — интервал биржи меняется на ходу, см. §2). Окно считается от текущего момента назад, не от последнего расчёта. Для 8-часовых монет окно 4 ч чаще всего пустое — тогда «—», а не 0. Пометка «!» — история неполная (нет последнего расчёта по слову биржи или дыра внутри окна).
- **Курсовой спред** — `mid_A/mid_B − 1` в бп, плюс полуспреды книг по отдельности. Это цена входа-выхода, её и надо сравнивать с накоплением фандинга.
- Пары с пометкой «не тот актив» показываются серым в конце таблицы, спред у них не считается.

## 6. Дашборд

Одна страница, тёмная/светлая тема, без внешних скриптов, localhost-only на VPS (порт **8792**, у lphedge 8791), доступ через ssh-туннель с Мака (`ControlMaster` уже настроен). Страница рендерится на каждый запрос из состояния коллектора; вверху — возраст данных по каждой бирже и красный чип, если старше 60 с или история неполная.

**Формат — эталон владельца (10.09, скрин чужого скринера): одна цифра в ячейке, без подписей, без годовых, спред в %, а не в бп.** Сортировка кликом по заголовку (по умолчанию |текущий|); фильтры: «со спотом Binance», «скрыть не тот актив», интервал A/B, |текущий| ≥ X (значения по умолчанию нет), поиск по монете.

| колонка | содержание |
|---|---|
| Монета | базовый актив; «≠» серым — не тот актив (строка в конце, серая) |
| Биржа | `aster ▼ \| binance ▲` — имена ссылками на страницу токена (Aster pro-режим, Binance futures; `spot` — спот Binance, если есть); стрелка вниз — шорт, вверх — лонг |
| Aster / Binance | предсказанная ставка за интервал биржи, как она показана на бирже |
| Текущий | спред за период пары = (час A − час B) × период; при равных интервалах — просто разность ставок |
| Курсовой | цена A \| цена B (миды книг, без книги — марки) и под ними разница в % за один токен |
| Период | больший из интервалов; если различаются — `4 (1\|4)` |
| ~~Отклонение~~ | убрано владельцем 12.09 («откл не нужно») |
| Комиссия | круг тейкером на обеих ногах по прайс-листу (`config.FEES_TAKER`, VIP0; тир счёта поправить там) |
| 4 ч · 1 день · 3 дня · 1 неделя · 1 месяц | сумма спреда за окно в %; «!» — история неполная |

**Два списка, переключатель справа сверху** (владелец 10.09: «чтоб не мешать всё в один список»). Выше описан режим **futures/futures** (строка = монета, перп Aster против перпа Binance). Второй режим — **spot/futures**: строка = СДЕЛКА «лонг спот Binance + шорт перп», перп — Aster или Binance, поэтому у монеты может быть две строки (на 10.09: 622 строки — 259 с перпом Aster, 363 с перпом Binance; 106 перпов Binance есть только здесь, их история собирается тем же обходом).

| колонка (spot/futures) | содержание |
|---|---|
| Монета | базовый актив; «≠» — перп и спот разные токены (с 12.09 — по составу индекса и контрактам, не по цене; «?» — не проверено, «⊘» — ввод/вывод монеты на споте закрыт) |
| Биржа | `binance spot ▲ \| aster ▼` — обе ссылками на страницу токена; структура фиксирована: спот только в лонг |
| Текущий | предсказанная ставка перпа за его интервал; плюс — платят нам, минус — мы (разворот требует шорта спота, в фазе 1 его нет) |
| Курсовой | цена спота \| цена перпа, под ними базис перп/спот − 1 за один токен (множители 1000X сняты) |
| Период | интервал перпа |
| ~~Отклонение~~ | убрано владельцем 12.09 |
| Комиссия | круг тейкером: спот ×2 + перп ×2 по прайс-листам своих площадок (Binance-спот + Aster 0.28 %, + Binance 0.30 %; KuCoin — по классу пары) |
| окна | сумма расчитанных ставок перпа за окно |

Сортировка у каждого режима своя: в futures/futures по модулю (знак лишь выбирает сторону), в spot/futures со знаком (зарабатывает только плюс). Фильтры режима: sf — перп (все/aster/binance), период; ff — со спотом, интервал A/B. Общие — скрыть «не тот актив», текущий ≥ X (в ff — по модулю), поиск. Режим, сортировки и фильтры запоминаются в браузере.

**Правки владельца 10.09 (вечер) — действуют поверх таблиц выше:**
- **Третья площадка — Hyperliquid.** Площадки добавляются ПО ОДНОЙ, в порядке, который называет владелец («а не сразу все»); порядок в `config.PERP_VENUES` = порядок добавления = ориентация пары (A — раньше в списке).
- futures/futures: строка = пара площадок на монету (aster|binance, aster|hyperliquid, binance|hyperliquid). Колонки «Aster / Binance» заменены одной **«Фандинг»**: `ставка A | ставка B` в порядке колонки «Биржа», каждая за интервал своей биржи. **Спота во futures/futures нет** — ни ссылки `spot`, ни фильтра «со спотом». Фильтры: биржа (строки, где она участвует), период.
- spot/futures: перп может быть и на Hyperliquid (спот по-прежнему Binance).
- **Сортировка во всех колонках со знаком**: первый клик — плюс сверху, второй — минус сверху; пустые «—» всегда в конце; «не тот актив» — в самом конце. Стрелка ↓/↑ у активной колонки.
- **Курсовой: проценты крупно сверху, цены мелко под ними** («проценты важнее»).
- **«Текущий», «Фандинг», «Отклонение» — В ЧАС** («приводи к 1ч»): за родной интервал часовой Hyperliquid проигрывал 8-часовым в 8 раз — его лучшая строка в spot/futures стояла 398-й из 768, в пересчёте на час — 3-я. «Период» — родной интервал справочно. Старые `ff_snapshots.spread` пересчитаны в час один раз (метка `meta.ff_spread_unit`), чтобы ряд не смешивал единицы.
- «Не тот актив» (до 12.09) сравнивал ИНДЕКСЫ перпов, а не марки: марк включает базис (ревью 10.09). С 12.09 цена в решении не участвует вовсе — см. «Сделано 12.09 (ночь)» ниже.
- Полнота окна: «!» ставится и тогда, когда история начинается позже начала окна, а глубину мы у биржи не подтверждали (`leg_depth`). Бэкфилл запускает проверка полноты — при старте, для новых ног часовой пересборки и повторно для упавших. Честно короткая история нового листинга — без «!».
- Интервалы fundingInfo обновляются раз в 15 мин (Aster меняет их на ходу); снимки — раз в 15 мин (`ff_snapshots`, `sf_snapshots`, с флагом «не тот актив»); старые `snapshots` писали NULL в ценах и дочищаются по сроку.
- Hyperliquid (замер 10.09): 234 перпа, 56 делистнуты; ставка часовая, `metaAndAssetCtxs` одним вызовом; история по 500 строк, пакетной нет — досбор добором раз в 15 мин; k-монеты ×1000; цены книги — `impactPxs`; комиссия тейкера 0.045 % (прайс-лист).

**Тестировщик и его первые выводы (11.09):**
- `funding_bot audit --venue hyperliquid|binance|aster` — запуск на VPS по команде владельца (порядок: Hyperliquid → Binance → Aster). Независим от кода коллектора: берёт у биржи то, из чего её сайт рисует список рынков (все dex Hyperliquid, все типы контрактов и квоты Binance/Aster), сортирует по фандингу как сайт и сверяет присутствие (с причиной отсутствия), ставки (с перепроверкой на следующем тике), историю расчётов и суммы окон, хвосты. Отчёт — `runtime/audit/<площадка>-<время>.md/.json`.
- Первый прогон: история верна 1-в-1 на всех трёх (150 рынков, 0 пропусков/лишних/значений). Найдено: не собирались 139 HIP-3 рынков Hyperliquid (xyz/para/io/mkts — 14 из топ-20 по фандингу), 190 TradFi-перпов Binance (NATGAS №1), USD1-листинги Aster (GPROUSD1 №2); баг полноты — нога со сменой интервала 4 ч → 1 ч навсегда считалась «мелкой», и шаг полноты, уходя в бэкфилл, ни разу не делал добор последнего часа (99 монет Hyperliquid без последнего расчёта).
- Исправлено: HIP-3 собираются (основной dex каждый тик, HIP-3 — каждый, чей снимок старше 40 с; до проверки исправлений 11.09 — по одному за тик); `TRADIFI_PERPETUAL` берётся; квоты USDT/USDC/USD1/U, но на площадке одна монета — один рынок (USDT, иначе USDC, USD1, U); ключ пары — `A:символ|B:символ` (у монеты бывает несколько рынков на площадке — xyz:NET и para:NET); «Биржа» для HIP-3 — `hyperliquid·xyz`; допуск глубины — 8 ч; добор и бэкфилл одним фоновым заданием; бэкфилл по запросу полноты всегда с пола; проверка полноты в hh:10 после часового расчёта; короткий добор Hyperliquid раз в 1.5 с.
- Не решено (вопрос владельцу): комиссии HIP-3 рынков — в `FEES_TAKER` стоит прайс-лист основного dex Hyperliquid; HIP-3 deployer может брать больше.

**Правки владельца 12.09 (скрин чужого скринера — «целевой вариант отображения комбинаций бирж»):**
- **«Отклонения» нет** («откл (отклонение) не нужно»): колонка и расчёт убраны из обоих режимов.
- **Споты Gate, KuCoin, Bitget** («нужны споты gate, kucoin, bitget») — после Binance, в этом порядке (`config.SPOT_VENUES`). spot/futures = спот любой спот-площадки + перп любой перп-площадки; ключ сделки `спот_площадка:символ|перп_площадка:символ` (флаги «не тот актив» прежней версии подхватываются по старому ключу). Клиенты — `spot.py`; что считается торгуемой парой, комиссии и ловушки каждой биржи — в его шапке (исследование API 11–12.09 с независимой перепроверкой). Комиссия спота — своя у площадки (VIP0: Gate 0.1 %, Bitget 0.1 %), у KuCoin — у каждой пары по классу A/B/C (0.1/0.2/0.3 %). На 12.09: сделок spot/futures 3631 (Binance 775, Gate 1049, KuCoin 986, Bitget 821) по 552 монетам.
- **Комбинации монеты — группой**: видна лучшая по текущей сортировке, под монетой «N спредов ⌄», клик раскрывает все (ячейка монеты на всю группу); монеты идут по своей лучшей комбинации; раскрытые помнятся в браузере. Фильтр «спот» в spot/futures.
- Одинаковый тикер ≠ та же монета и на спотах: EDGE на Gate — Definitive (индекс перпа берёт Gate EDGEX = edgeX) — «не тот актив». Поправка 12.09 (контракты): ESPORTS на Gate (Yooldo), GUA на Gate (SUPERFORTUNE), SIREN, MANTRA на Bitget — ТА ЖЕ монета, у них закрыт ввод на споте, поэтому цена ушла от перпа; ценовой флаг их ошибочно прятал. Теперь такие строки обычные, с пометкой «⊘ ввод закрыт».
- Таблица ~5 МБ: веб отдаёт её сжатой (gzip), страница по-прежнему обновляется раз в 10 с.
- **Сверка «покрываем ли всю линию» (скрины чужого скринера 12.09):** фандинги и окна совпадают (текущий, 1д/3д/1н —
  до округления; 1м у них 31 сутки). Дыры в линии и что сделано: (1) **токенизированные акции** на спотах (Binance …B,
  Gate …X/…ON/…G, KuCoin …X, Bitget r…/pre…) против перпов на акции (Binance TradFi, Aster STOCK/ETF, Hyperliquid HIP-3):
  у них CRCL — 16 спредов, у нас было 0; теперь токен спота несёт тикер акции (`alt_base`) и даёт сделку только перпу
  на акцию (`tradfi`), подпись «gate·CRCLX» — CRCL 18 комбинаций, всего +815 сделок; (2) **та же монета под другим
  тикером** (SPORTFUN = FUN на Gate/KuCoin/Bitget) — проверенная таблица синонимов `config.SPOT_ALIASES`;
  (3) спот **Bybit** и ещё одна перп-биржа у них есть, у нас нет — только по команде владельца. В обратную сторону у нас
  больше: 龙虾 на KuCoin (код LONGXIA), POWER на Gate, комиссия KuCoin по классу пары (у них везде 0.1 %).
- **Классы активов** (исследование правил со скептиком, 12.09): у перпа класс crypto / equity / commodity / index / preipo
  (Binance — TRADIFI + underlyingType, Aster — underlyingSubType, Hyperliquid — perpCategories). Пары futures/futures —
  только внутри класса (Quant QNT ≠ Quantinuum xyz:QNT; мем-CAT ≠ акция Caterpillar); тикеры акций приведены к биржевым
  (QNTX→QNT, BBX→BB, STXX→STX, SKHX→SKHYNIX, SMSN→SAMSUNG; GOLD→XAU, SILVER→XAG). spot/futures: монета — та же база и
  проверенные синонимы (`config.SPOT_ALIASES`, по биржам: AI = Gensyn только на KuCoin/Bitget, RON = Ronin только на Gate);
  акция — только токенизированные акции (признак биржи: тег bStocks у Binance, класс валюты у Gate, рынок Stocks у KuCoin,
  r-префикс у Bitget; pre-IPO не берём); сырьё — только токены золота (PAXG, XAUT, XAUM); индексам спота нет. На 12.09:
  spot/futures 4509 (монеты 3659, акции 823, золото 27), futures/futures 1035 (−6 ложных межклассовых, +14 новых).
  На странице у не-монет подпись класса («акция», «сырьё»); у спота под другим тикером — «gate·CRCLX».
  Валюта (xyz:EUR) — со спотом той же валюты. Тестировщик после классов видел «БАГ» у одинаковых тикеров разных
  активов (акция BNC Binance и монета BNC Bifrost ×400; монета MAX Aster и акция MAX Gate) — теперь пара по тикеру
  засчитывается ему, только если цена за токен сходится (±10 %), иначе статус «тикер совпадает, актив другой — по замыслу».

**Решения владельца 12.09 (вечер) — в работе:**
- Токены, в которых несколько акций (NFLXX/NFLXON = 10 NFLX, CRWDX = 4, TQQQX = 2), — пересчитывать на одну акцию («да»).
- «Не тот актив» — «отмечать по контрактам, где это возможно, либо по другим признакам, но не по курсу»: ценовой флаг
  (3 %, три тика) снимается; признаки — контракт токена, состав индекса перпа (Binance constituents), метки бирж.

**Сделано 12.09 (ночь):**
- Ценовой флаг удалён целиком (гистерезис, таблица `identity` больше не пишется — оставлена ради отката). Решение —
  `identity.py` (чистое), сеть — `identity_src.py`. Источники: состав индекса Binance `/fapi/v1/constituents` и Aster
  `/fapi/v3/indexreferences` (раз в сутки, новый перп — сразу, пачками по 80), списки монет спотов с контрактами во
  всех сетях и именами (Gate, KuCoin, Bitget, Binance + список Alpha, раз в час), оракул Hyperliquid по документации
  (спот Binance/KuCoin/Gate), описания HIP-3; для перпов, чей индекс — только DEX-пулы (Aster AI, MEME — токены сети
  Robinhood), — поиск пула DexScreener, принимается только однозначный. Всё хранится в БД (`ident_src`): после рестарта
  вердикты сразу.
- Вердикт строки: «тот» (рынок спота в индексе перпа, общий контракт, то же имя, родная сеть), «не тот» (индекс берёт
  другой рынок той же площадки; контракты и имена разные), «не проверено» (с причиной). Отсутствие рынка в индексе —
  никогда не «не тот». Акции/сырьё/валюта — по признакам площадок; pre-IPO — «не проверено» (единицы разные).
- Сверка на живом срезе 12.09: спот/перп крипто — тот 3619, не тот 12 (Aster AI ×4, Aster MEME ×4, Gate EDGE ×2,
  KuCoin/Bitget UP ×2), не проверено 28; перп/перп — 715 / 2 (MEME) / 1. Из 31 строки, которую прятал ценовой флаг,
  17 — та же монета с закрытым вводом (ESPORTS, SIREN, GUA, MANTRA, ONE, RVN…).
- Новая пометка «⊘» — ввод и/или вывод монеты на споте закрыт во всех сетях (на 12.09 — 93 сделки): цена может уйти от
  перпа, переложить монету нельзя. Это не «не тот актив».
- Множитель токенов акций (решение «да»): xStocks — `api.xstocks.fi …/multiplier` (NFLXX 10, CRWDX 4, TQQQX 2.009,
  AZNX 0.511 — обратный сплит), Ondo — `SyntheticSharesOracle.getSValue` на BSC (NFLXON 10); Binance bStocks, Bitget r…,
  Gate …G — 1 (политика площадок, проверено ценой при исследовании). Раз в час; не пришёл — прежний. Подпись сделки —
  «gate·NFLXX ×10», курсовой — за акцию. Дивидендный дрейф тоже учтён (HDX 1.023, KOON 1.020).

**Правки владельца 12.09 (вечер), скрины чужого скринера:**
- «вместо периода добавить биржи вот так (выбор бирж)» — фильтр «период» убран в обоих режимах; «биржи» — кнопка и список
  с галочками: «Все», дальше биржи по алфавиту со значком (Aster, Binance, Bitget, Gate, Hyperliquid, KuCoin). Сделка
  видна, если ВСЕ её площадки среди выбранных; спот и перп Binance — одна биржа. Фильтры «спот», «перп», «биржа» оставлены.
- «замени текущий на 1d где по умолчанию стоит >0.5%» — вместо «Текущий ≥» фильтр «1d >» по сумме ставок за сутки
  (колонка «1 день»), по умолчанию 0.5 %; во futures/futures по модулю. Фильтр только по числу: «не тот актив» прячет
  свой переключатель, устаревшие остаются бледными внизу.
- «вот эти предупреждения тут зачем?» — строка состояния теперь только при неполадке: данные или площадка старше минуты,
  пауза после 429, строки на старых ставках, дыры в истории, диск, ошибки шагов. Всё в порядке — строки нет. Число «не тот
  актив» — у переключателя «скрыть». Плановый досбор после часового расчёта (Hyperliquid — 290 ног по одной, ~7.5 мин)
  больше не считается дырой ни в чипе, ни в «!» у окон: `funding.completeness` → `catchup` (последний расчёт моложе
  `CATCHUP_S` = 25 мин); добор при этом идёт как раньше. Позже 25 мин — это уже дыра и видна.
- «биржи спот и futures отмечать нужно галочками какие вкл какие выкл» — два списка с галочками: «спот» (Binance, Bitget,
  Gate, KuCoin, OKX DEX) и «фьючерсы» (Aster, Binance, Hyperliquid); спот и фьючерс одной биржи — отдельно. Общий список
  «биржи» и выпадающие «спот/перп/биржа» убраны.

**OKX DEX — спот-нога (владелец 12.09: «в спот добавить okx dex», «почему для этих монет нет okx dex? надо сделать»):**
- Ключ владельца (ключ, секрет, пароль) — только в `.env` на VPS, служба коллектора читает его через `EnvironmentFile`;
  без полного ключа площадки нет вовсе (ни строк, ни запросов). Проверено ключом 12.09: у агрегатора 35 сетей, 56/501/4663
  есть; цены, ликвидность, котировки в обе стороны работают.
- Токен — только доказанный: контракт монеты на BSC / Solana / Robinhood из списков монет наших спотов, токен Binance
  Alpha или пул DEX из состава индекса перпа (`identity.dex_tokens`). Поиска по тикеру нет. Aster AI и MEME получают свои
  настоящие токены сети Robinhood (Artificial Inu, A Meme Coin).
- Только перпы `config.PERP_VENUES`; строка — при ликвидности токена от $50k; мост — родная монета другой сети (BTC →
  BTCB) или признак в имени (Binance-Peg, Wormhole…), с пометкой «мост» и обычным сдвигом к перпу в подсказке.
- Цена DEX — пачкой раз в 3 мин (своя свежесть 10 мин); ликвидность и имена — раз в час; котировки клипа $15 в обе
  стороны — по очереди, самые старые первыми, до 60 с на задание (ключ 1 запрос/с): полный круг ~35 мин.
- «Комиссия» DEX-строки — всё включено: круг по цене (пул + удар в обе стороны) + газ входа и выхода + налог токена
  + 2 × тейкер фьючерса; разложение — в подсказке. Котировки ещё нет — «—».
- Живой прогон 12.09: 367 токенов-кандидатов (BSC 283, Solana 77, Robinhood 7), ликвидных 266, строк 545 (мостов 94);
  TRUMP (Solana) круг 0.10 %, газ 0.007 %; Artificial Inu (Robinhood) круг 0.24 %, газ 0.70 %; ESPORTS (BSC) круг 0.39 %.
  Квота Market API при таком темпе ~60K вызовов в месяц из 100K бесплатных.
- Не сделано (следующий шаг): токены акций на DEX (xStocks на Solana, Ondo на BSC, Robinhood Tokens) — нужен свой
  реестр и множители; стоимость завода стейбла и газа на сеть (вывод с бирж) — порог владельца.
- «В спот добавить OKX DEX» — только по токенам, у которых есть фьючи на наших перп-биржах (Aster, Binance, Hyperliquid;
  Lighter — «пока без», Aster — оставить). Для DEX-ноги учитывается комиссия сети (вход и выход). Список перп-бирж
  «может быть расширен в будущем по моей команде» — вселенная токенов OKX DEX строится от `config.PERP_VENUES`, а не
  зашитым списком: новая перп-биржа сама добавляет свои монеты в OKX DEX.
- OKX DEX (ответы владельца 12.09): сети **Solana, BSC, Robinhood**; строка появляется при ликвидности токена на DEX
  **от $50k** (порог владельца); мостовые/обёрнутые версии (WBTC/BTCB, Binance-Peg, Portal) — **считаются, с пометкой
  «мост»** и постоянным сдвигом цены рядом; «Комиссия» DEX-строки — **всё включено** (пул + удар + газ туда и обратно +
  налог токена + тейкер перпа, расшифровка в подсказке), у CEX — как есть. Котировка — на клип $15 (верх диапазона
  владельца $10–15). Ключ OKX Developer Portal владелец создаёт сам, в `.env` на VPS: OKX_DEX_API_KEY / OKX_DEX_SECRET /
  OKX_DEX_PASSPHRASE; без ключа площадка выключена (API отвечает 401, code 50103 — проверено с VPS 12.09).
  «Без удара по стакану» на DEX не бывает — своп всегда в пул; удар на клип показывается.
- Проверка перед выкатом (4 атакующих + скептики, 9 подтверждённых, все закрыты): перп Binance 1MBABYDOGE теперь
  находит спот BABYDOGE (множитель 1M = ×10⁶); сделка без свежей книги своего спота — «устарела» (молчащая спот-биржа
  не становится лучшей комбинацией монеты); при сортировке по Монете/Периоду/Комиссии первой в группе идёт лучшая по
  текущему; настройки страницы прежней версии сохраняются; `funding_bot top` не падает; тестировщик не выдаёт ложный
  «БАГ» после того, как коллектор по замыслу отбросил пару, и видит ошибки KuCoin/Bitget в теле ответа; снимки со
  спотами — ~1.6 ГБ за 30 суток хранения.

**Новые перп-биржи 12.09 (владелец: «добавить фьючи kucoin bitget и gate, lighter; lighter нужны 2 вида: мейн и robinhood,
оба спот и фьюч»):**
- Порядок владельца: `PERP_VENUES` = aster, binance, hyperliquid, **kucoin, bitget, gate, lighter, lighter_rh**; споты + **lighter_spot,
  lighter_rh_spot**. Клиенты — `kucoin_fut.py`, `bitget_fut.py`, `gate_fut.py`, `lighter.py` (в шапке каждого — замеры, лимиты,
  ловушки). Lighter — два независимых инстанса (основной, квота USDC; Robinhood Chain, квота USDG): один тикер там — разные
  рынки; перп Robinhood подписан «lighter·rh».
- Живой прогон с Мака 12.09 ~16:00 UTC: KuCoin 677 перпов (4 ч ×432, 8 ч ×245), Bitget 786, Gate 981, Lighter 200 (из 217
  активных, 17 — только на закрытие) и Robinhood 57; ставки и книги — у 100 % своих рынков (у Lighter основного книг 197 из 200).
  Споты Lighter: 10 и 27 пар (25 — токены акций). Вселенная: futures/futures 1035 → 8543, spot/futures 4510 → 11646; пар
  между классами — 0.
- Интервал: KuCoin, Bitget, Gate меняют его на ходу (TENCENT 8 → 4 ч, IOST 8 → 1 ч, STORJ 4 → 1 ч; у KuCoin TRUST — сетка 4 ч со
  сдвигом 3 ч) — не «неизменные»; Lighter — каждый час на часе (`FIXED_INTERVAL_H` = 1). Пакет «последние расчёты по всем»
  есть только у KuCoin; остальные доводятся посимвольным добором — `REPAIR_MAX_CALLS` 1000 → 3000 (в 00/08/16 UTC ~2300 ног).
- Комиссия тейкера: KuCoin 0.06 %, Bitget 0.06 %, Gate 0.05 % (VIP0; поле API 0.075 % — не тариф), Lighter 0 % (стандартный
  счёт, оба инстанса, спот тоже).
- Ссылки — из самого инструмента (Bitget/Gate: 龙虾 в %-кодировке; Lighter: kPEPE, SPY_USDC), иначе шаблон; у Lighter Robinhood
  страницы рынка нет — имя без ссылки. Галочки: фьючерсы — Aster, Binance, Bitget, Gate, Hyperliquid, KuCoin, Lighter, Lighter
  Robinhood; спот — плюс Lighter и Lighter Robinhood.
- Имена сырья и валют — по определению (марки за единицу сошлись в пределах 0.2 %, это лишь проверка единиц): XCU → COPPER,
  PLATINUM → XPT, PALLADIUM → XPD, BRENTOIL → BZ; EURUSD → EUR, GBPUSD → GBP, USDJPY → JPY. Индексы с разными именами
  (SPX500/SP500, NAS100/NDX100/XYZ100) не сводятся — определением не доказать.
- «Тот ли актив»: состав индекса Gate и Bitget клиенты отдают, но коллектор его пока не собирает; у KuCoin в индексе только
  имена бирж. Пока — слабое свидетельство: монета спота ТОЙ ЖЕ биржи с тем же кодом (как «venue_code» у Binance), остальные
  споты решаются контрактом. Lighter — оракул площадки: «не проверено», пока нет имени (tokenlist) или контракта. Разные
  контракты при «имени», равном тикеру, — «не проверено», а не «≠» (KuCoin NIGHT = «NIGHT» на Cardano и Binance «Midnight»
  на BSC — одна монета: индекс Binance NIGHTUSDT берёт KuCoin NIGHT-USDT). Срез sf новых перпов: KuCoin 1783 тот / 212 не
  проверено; Bitget 1643 / 367; Gate 2079 / 142 / 2 «≠» (UP: у Gate — Unitas, на KuCoin/Bitget — Superform); Lighter 151 / 453
  и Robinhood 124 / 80 (с именами tokenlist было бы 492 / 112 и 162 / 42).
- Риски выката: Gate `/futures/usdt/tickers` — 461 КБ без gzip, с Мака 0.4–22 с (список контрактов до 45 с) при дедлайне тика
  8 с — замерить на ireland; таблица 5.2 → 19.5 МБ (gzip 0.8 МБ); снимки ~4× — ~7 ГБ за 30 сут при нынешнем `SNAPSHOT_S`
  (решение владельца). Коллектору ещё нужно: интервалы нативных клиентов (`funding_intervals`), состав индекса Gate/Bitget
  (`index_legs`), имена перпов Lighter в проверку (`perp_names`).

**Новые перп-биржи 13.09 (владелец: «добавь биржи backpack, variational, edgex, extended, pacifica, apex во фьючи»):**
- Порядок владельца: `PERP_VENUES` = …, lighter_rh, **backpack, variational, edgex, extended, pacifica, apex**; только перпы, споты
  прежние. Клиенты — `backpack.py`, `variational.py`, `edgex.py`, `extended.py`, `pacifica.py`, `apex.py` (в шапке каждого — замеры,
  лимиты, ловушки с метками [L] живой замер / [D] документация / [A] допущение). Только публичные точки, без ключей и счетов.
- Единицы ставки: Backpack, Extended, Pacifica, ApeX — доля за 1 ч как есть; edgeX — за 4 ч; Variational публикует ГОДОВУЮ ставку —
  клиент переводит в долю за интервал (× интервал / 8760). Живой прогон с Мака 12.09 22:41 UTC, ставка в час у всех 14 бирж рядом:
  BTC от −1.26e-5 (edgeX) до +1.30e-5 (Extended), ETH от −7.8e-6 (ApeX) до +1.30e-5 (Extended) — ни одна не выбивается в 8× или 24×;
  медиана |ставки в час| по общим монетам с Hyperliquid и Bitget — 1.00 у всех шести (Extended 1.04: базовая 1.3e-5 против 1.25e-5).
- Живой прогон: инструменты Backpack 89, Variational 547, edgeX 170, Extended 323, Pacifica 76, ApeX 125; ставки — у 100 % своих
  рынков; книги — edgeX 170/170, ApeX 124/125 (у USO нет продавцов), Variational 547 (котировка RFQ на 1000 USDC), Extended 259/323
  (64 акции вне часов торгов — цена по марку), Backpack 81/89 через 20 с (поток шлёт только изменения, тихие рынки — REST /depth по
  5 за тик), Pacifica 0 → 76 через 10 с (WebSocket без снимка). Ошибок и 429 — 0. Вселенная: futures/futures 8541 → 17312,
  spot/futures 11646 → 15778; пар между классами — 0, пар «rwa» — 0.
- Интервал: «неизменные» 1 ч — Backpack, Extended, ApeX (30 сут без разрыва у всех рынков). Не «неизменные»: Variational (1/4/8 ч,
  меняет на ходу), edgeX (интервал у контракта свой, сейчас 4 ч у всех), Pacifica (1 ч, но площадка пропускала расчёты 05.09 и 09.09
  00:00 UTC — с «неизменным» это были бы вечные дыры). Интервалы на ходу отдают Backpack, Variational, edgeX (`funding_intervals`).
- Комиссия тейкера (базовый тир): Backpack 0.05 %, Variational 0 % (издержка — спред котировки RFQ, колонка её не видит), edgeX
  0.045 % (живой замер), Extended 0.025 % (документация), Pacifica 0.04 %, ApeX 0.05 %.
- Ссылки — из самого инструмента (Backpack BTC_USD_PERP, edgeX %-кодировка 哈基米, Extended по uiName: 1000PEPE → kPEPE-USD), шаблон
  `URLS` — запасной; у Extended шаблона нет (по имени API 40 переименованных рынков молча показали бы BTC-USD). Галочки фьючерсов:
  ApeX, Aster, Backpack, Binance, Bitget, edgeX, Extended, Gate, Hyperliquid, KuCoin, Lighter, Lighter Robinhood, Pacifica, Variational.
- «Тот ли актив»: все шесть — перпы на оракуле площадки (`identity.ORACLE_PERPS`), состава индекса в API нет ни у одной. Свидетельство —
  только заявленное имя рынка (`perp_names`): Backpack (displayName), Variational (полное имя), Extended (description), ApeX (tokenName);
  у edgeX и Pacifica имён нет — «не проверено (оракул)». У новых площадок имя, равное тикеру («MKR», «Pepe» у 1000PEPE), — не имя;
  у Lighter имена идут как 12.09 (такой фильтр снял бы 74 из 200 и 16 из 57, их «тот по имени» стали бы «не проверено») — **решение
  владельца**. Ловушка в срезе: EDGE на Gate (Definitive) против EDGE новых бирж (токен edgeX) — курсовой −88 %, «тот» не ставится.
- Пара ни с чем: класс «rwa» (рынок не распознан: ApeX CXMT/UNITREE/CBRS, Variational B3) — раньше два «rwa» с одним тикером на разных
  биржах вставали в пару, теперь нет; `config.PERP_UNPAIRED` — рынки с чужой единицей цены: Extended XIAOMI-USD — 26.28 при 3.36 у
  Bitget/Gate (×7.81 = курс HKD/USD, цена акции в гонконгских долларах), множителем не привести — без этого было бы 5 пар «тот же
  тикер в классе акция» с курсовым −87 %.
- Variational: публичной истории нет вовсе. История — своя книга расчётов клиента в памяти (последняя оценка ставки перед границей
  окна; оценка старше 15 мин, пропущенное окно или смена интервала — дыра, а не догадка); глубину с пола у неё не просим
  (`collector.own_history`), расчёты ложатся в БД пакетом `recent_history` примерно через 10 ч после старта, глубина копится сама,
  рестарт — дыра. Снимок за Cloudflare-кэшем: ставке 30–60 с, котировкам медиана ~50 с, до 90 с → свой предел свежести
  `STALE_S_BY_VENUE` = 180 с (с общими 60 с большинство строк было бы «устаревшими») — **решение владельца**.
- Нагрузка: ноги истории 4115 → 5412 (+1295). Variational — 525 без сети; остальные 770 ног — бэкфилл 30 сут ≈ 1625 вызовов (ApeX 976 —
  8 страниц на ногу, Extended 319, edgeX 165, Backpack 89, Pacifica 75 по 7.5 с ≈ 9.4 мин) ≈ 16 мин одних пауз → 4–5 проходов
  обслуживания по 5 мин. Добор каждый час — ещё ~606 вызовов (пакета «все монеты» нет у Backpack, Extended, Pacifica, ApeX; у edgeX
  есть) → `REPAIR_MAX_CALLS` 3000 → 4000 (в 00/08/16 UTC вышло бы ~2900+). Тик: +~9 REST-запросов за 10 с (Backpack до 6:
  /markPrices и до 5 /depth тихих рынков; Variational, Extended, Pacifica по 1; edgeX и ApeX — 0, всё по WebSocket) и 4 WebSocket.
  Входящий трафик (замеры клиентов с Мака): Extended ~1 МБ на тик ≈ 8.4 ГБ/сут, edgeX ~110 КБ/с ≈ 10 ГБ/сут, ApeX ~52 КБ/с ≈ 4.5 ГБ/сут,
  Variational ~281 КБ распакованного на тик — вместе ~25 ГБ/сут. Строк таблицы ×1.64 (20187 → 33090): table.json ~19.5 → ~32 МБ, снимки
  в БД ×1.6 [допущение по пропорции]. На ireland не замерено — проверить до включения.
- Открыто: US500 у Variational — акция SPY, не сведена со SPY; B3 у Variational — «rwa» до проверки; OPENAI у Lighter против
  Variational/Extended — курс 6–9 % (pre-IPO, разные оракулы); `audit.py` новых бирж не знает; у ApeX статус `ss=5` (USO) не фильтруется;
  GRAM у ApeX не сведён с TON; выплата edgeX в момент fundingTime или интервалом позже — не проверено (нужен счёт).

**Внешнее ревью 11.09 (`hyper/reviews/FUNDING_REVIEW_20260911.md`) — 8 находок, все закрыты тем же днём:**
1. *Восстановление после простоя.* У ноги курсор непрерывности `leg_depth.synced_to`: история в БД полна на [checked_from, synced_to]. Добор идёт с курсора — весь пропуск, а не только последний час (было: 22 расчёта из 24 выдавались за полные сутки). Курсор двигают только сборы без разрыва: с пола, с synced_to+1 и пакет «последние расчёты по всем» — для символов, попавших в пакет (проверено: пакет Binance ~7 ч и Aster ~3 ч содержит все символы, включая TradFi). Курсор = max(сейчас − льгота, последний полученный расчёт). Ноги прежней версии без курсора один раз пересобираются с пола в очереди глубины. У Hyperliquid (интервал всегда 1 ч) под курсором дополнительно проверяется наличие прошлого часа и разрывы больше часа.
2. *Одна плохая нога не держит остальных.* Обслуживание — одно фоновое задание: сначала свежие расчёты всех ног (всегда), потом глубина — не дольше 5 мин за проход. Падающая нога повторяется через 15 мин, 30, 60 … до 4 ч; площадка в лимите пропускается только сама.
3. *Свежесть.* У каждой цены метка наблюдения и номер снимка: книга используется, только если она из того же снимка, что ставка, иначе цена по марку («≈» в «Курсовом»); ставка старше минуты — строка бледная и в сортировке после годных, в снимках колонка `stale`. Чип площадки краснеет по самому старому из её источников.
4. *«Не тот актив»* засчитывает подтверждение, только если обе ноги принесли новые наблюдения; нет свежей цены — серия сбрасывается.
5. *«Отклонение»* — только при полной суточной истории обеих ног; иначе «—» (у свежего листинга сумма с листинга ÷ 24 — не средний час суток).
6. *Медленная площадка не держит тик.* Опрос параллельный, тик ждёт не дольше 8 с; тиковые запросы — одна попытка, таймаут 8 с (было до 51 с на площадку).
7. *Фоновое задание не залипает:* БД открывается внутри защищённого блока, флаг «занято» снимается при любой ошибке.
8. *Выкат:* тесты на Маке (с тестом страницы) → код в `funding_bot.next` и тесты там → снимок `funding_bot.prev` → переключение → строгие проверки (оба сервиса, `/status`, тик нового процесса) → откат при неудаче. Ротация журналов (logrotate, 7 дней, 50 МБ). Тесты настоящего HTTP-клиента Hyperliquid.

**Проверка исправлений перед выкатом (11.09, вечер): 4 атакующих (восстановление, потоки, выкат, страница) + скептики — 13 находок, 9 подтверждены, все закрыты (`tests/test_fix_verification.py`, выкат — в `test_review_regressions.py`):**
1. *Сеть только в пуле потоков.* У площадки два слота: тик и задание «вселенная / интервалы / пакетный досбор» (что из этого пора — одним заданием). Раньше вспомогательные шаги шли на главном потоке с тремя попытками по 15 с (зависший exchangeInfo — цикл стоит 51 с каждые 2 мин), а площадка, чей тик опоздал к дедлайну хоть на секунду, считалась «занятой» — её интервалы и досбор не шли вовсе (смена интервала 1 ч → 4 ч давала часовую ставку вчетверо больше, новые листинги не появлялись). Главный поток ждёт сеть не дольше 8 с за проход; разбор и запись в БД — на нём.
2. *В hh:10:30 проверка полноты ждёт пакетный досбор всех площадок* (не дольше 2 мин) — иначе каждый свежий расчёт уходил бы в посимвольный добор.
3. *Паузы повтора раздельные* у добора и у глубины: успешный добор ноги обнулял паузу её падающей пересборки с пола (длинный запрос каждые 15 мин вместо 15 → 30 → … → 4 ч), а растущая пауза глубины держала здоровый добор.
4. *HIP-3:* за тик обновляется каждый dex, чей снимок старше 40 с (было — по одному: при 5+ dex хвост старел дольше минуты, строки бледнели, «не тот актив» не подтверждался).
5. *Окна в льготу после расчёта:* пока расчёт, который уже был, не лёг в БД, окна ноги кончаются перед ним — иначе сутки были без одного расчёта (у 8-часовых ног 2 из 3), а «отклонение» врало на треть ставки.
6. *Выкат:* замок на сервере (второй одновременный выкат останавливается, а не подменяет `.next` первому); `.prev` — только проверенная версия (метка `.verified`), упавшее на полпути переключение → откат; установка пакета без сети; проверка узнаёт новый процесс по pid (`pid` в table.json = MainPID) и ждёт 20 с без рестартов (раньше проходила по таблице старого процесса); журналы принадлежат admin (systemd создавал их root:root — logrotate не мог их обрезать).

**Требования ревьюера к торговой фазе (записаны, не реализуются в фазе 1):** разность ставок — индикатор, а не PnL: выплата считается по размеру, цене (у Hyperliquid — по цене оракула) и валюте каждой ноги (USDC-маржа против USDT); для входа нужны цены нужной стороны и глубина книги на заданный размер, а не мид/impact; до ордеров — проверяемый реестр соответствия активов со статусом «не проверено»; явный контракт площадки (пакетная история, расписание фандинга, свежесть источников, множитель контракта, квота/валюта расчёта, тип цены).

Никаких оценок доходности «за вычетом всего» в фазе 1 — пороги и капитал владельца не заданы, значит и ожидаемый итог не считается.

## 7. Бюджет запросов

| вызов | биржа | период | вес | в минуту |
|---|---|---|---|---|
| premiumIndex (все) | A, B | 10 с | ~10–20 | ≤ 120 |
| ticker/bookTicker (все) | A, B | 10 с | 2–5 | ≤ 30 |
| spot ticker/bookTicker (все) | B spot | 10 с | 4 | 24 (лимит 6000) |
| fundingRate без символа | A, B | 15 мин | 1 | — |
| exchangeInfo + fundingInfo | A, B, B spot | 60 мин | 1–20 | — |
| бэкфилл fundingRate?symbol= | A, B | старт | 1 × 394 | Binance ≤ 500 / 5 мин |

Итого < 10 % бюджета обеих бирж. WebSocket `!markPrice@arr` — резерв на фазу 3 (позиции live), в фазе 1 не нужен: 10-секундного опроса хватает, а молчаливые обрывы сокета (урок HL) — отдельный класс проблем.

## 8. Инфраструктура, выкат, тесты

- Systemd: `funding_bot-collector.service`, `funding_bot-web.service`, `Restart=always`, журнал в `logs/`, ротация только в окно рестарта (не кроном — урок TWAP).
- Публичная ссылка (владелец 12.09: «публичный адрес», «чтоб у друга открывался», без пароля): `funding_bot-tunnel.service` — официальный cloudflared, быстрый туннель Cloudflare к 127.0.0.1:8792; входящих портов нет. Текущий адрес — `runtime/public_url.txt`; выкаты службу не трогают, адрес меняется только при её рестарте. Веб отдаёт страницу и `/data.json` из кеша, собранного раз на снимок таблицы (несколько зрителей не грузят сервер).
- Новые ноги (например, перп Aster, у которого из спотов только OKX DEX) сразу получают проверку полноты и бэкфилл глубины — «!» у них не висит до ближайшего досбора.
- `deploy/deploy.sh`: тесты на Маке → замок на сервере → rsync в `funding_bot.next` без `runtime/` и `.env` → **тесты там (красные останавливают выкат, боевая не тронута)** → `.prev` (последняя проверенная) → переключение → проверки по pid нового процесса → откат при неудаче. Кода без тестов в бою нет.
- Тесты на фальшивых ответах бирж (`tests/fakes.py`): приведение интервалов (1/2/4/8 ч) к часу; суммы окон при разных интервалах и пустом окне; дедуп `funding_events`; полнота n/N при дыре; ловушка «не тот актив»; нормализация `1000X`/`kX`; бюджет веса (тик пропускается при > 70 %); бэкфилл идемпотентен при повторном старте.
- Секретов в фазе 1 нет. Каталог `runtime/` и будущий `.env` не попадают в rsync и архивы.
- Мониторинг: `/status` отдаёт возраст тиков и истории; алерт-чип в дашборде. Отдельный сторож — по решению владельца.

## 9. Этапы и приёмка

| этап | результат | приёмка |
|---|---|---|
| 1. Клиент + вселенная | `instruments`, `pairs` по обеим биржам, ловушка тикеров | 394 ± пары, MEME/SLP помечены «не тот актив» |
| 2. Коллектор | тики 10 с, история 48 ч + досбор, снимки 5 мин | после рестарта — ни дубля, ни дыры; `health` живой |
| 3. Дашборд | таблица §6, сортировки, фильтры, свежесть | открывается через туннель < 1 с, возраст данных ≤ 30 с |
| 4. Выкат | systemd, deploy.sh с воротами, тесты зелёные | сутки без 429; история полная по всем парам |

После этапа 4 — неделя наблюдения: как живёт спред (переворачивается ли, сколько держится), чтобы владелец задал пороги для фазы 2 по данным, а не наугад.

## 10. Что дальше (не в этой фазе, зафиксировано на будущее)

- **Фаза 2, вход по команде.** Клипы по $10–15, сначала длинная нога, потом шорт (урок lphedge); на Aster — мейкер 0 % даёт основание ставить пост-онли (`GTX`) вместо тейкера, тонкая книга Aster — довод за то же. Декс-нога: комиссия сети только на вводе/выводе (ордера на Aster off-chain, газа не требуют — проверить на ключе). Ключ Aster v3 — подпись EIP-712 кошельком (`user`/`signer`/`nonce`), v1-ключи не выдаются с 25.03.2026. Binance — отдельный суб-аккаунт, чтобы не мешать зеркалу TWAP.
- **Фаза 3, позиция live.** Тики фандинга по обеим ногам с биржевых журналов, а не расчётом; знак и базис; закрытие подтверждает биржа.
- Все пороги и лимиты — из `runtime/owner.toml`, заданного владельцем.

## 11. Вопросы владельцу — закрыты 10.09

1. Название — `funding_bot`.
2. Длинная нога — сначала спот Binance; дексы Solana и EVM (Robinhood, BSC, Base, Arbitrum) следом, слой сменный.
3. Диск чистит владелец.
4. История — 30 суток.
