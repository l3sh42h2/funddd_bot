"""Telegram-бот владельца фазы 2 (trade_spec §6, отчёт telegram 12.09). Bot API через requests, без библиотек-обёрток.

Модули (от нижнего к верхнему):
  api.py     — TgApi.call(): таймауты, разбор ошибок (429/409/400/403), токен не попадает ни в текст ошибки, ни в repr;
  poller.py  — long-poll getUpdates: claim_update ДО смещения, живость = успешный возврат, сторож переподключения;
  sender.py  — очередь отправки: темп 1.05 с, 429 retry_after, резка на 4000, HTML с откатом в простой текст;
  parse.py   — чистый разбор русских команд (и /status /positions /stop /help), данные кнопок ok:<id>:<nonce8>;
  auth.py    — только владелец: classify(update), /start незнакомцу с лимитом, «устарела», CAS кнопки;
  views.py   — чистые русские шаблоны сообщений (HTML).

Проводка (диспетчер, исполнитель, SIGTERM) — в bot.py интеграции; здесь только чистые функции и классы.
Пакет не импортирует eth-account: ключи бота Telegram — это только TG_BOT_TOKEN, его маскирует keys.redact*.
"""
