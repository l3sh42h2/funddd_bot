"""Фаза 2: исполнение по команде владельца (DEX-лонг + перп-шорт). Спецификация — trade_spec (12.09).

Слои фундамента (от нижнего к верхнему):
  types.py   — структуры и протоколы ног (деньги и количества только Decimal или сырой int);
  tconfig.py — технические константы (не деньги): таймауты, allowlist роутеров OKX, RPC;
  owner.py   — строгий загрузчик runtime/owner.toml: «пусто = запрещено», перечитывается на каждую команду;
  keys.py    — ЕДИНСТВЕННОЕ место, где читаются приватные ключи; ворота режима dry/readonly/live;
  store.py   — runtime/trade.db (WAL): запись до внешнего вызова, результат после.

Пакет намеренно не импортирует подмодули: eth-account (extra `trade`) нужен только keys.py и подписантам,
а сухой режим и тесты коллектора не должны от него зависеть.
"""
