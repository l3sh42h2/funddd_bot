# M4 accounting replay — 2026-09-14

- Исполнитель: Codex, M4 replay helper (`gpt-5.6-sol`, high).
- Назначенный ревьюер: отдельный `gpt-6-astra`, xhigh, назначен владельцем; ревью ещё не выполнено.
- База: `aaf57374c2ef7924e81248bed81d001cebde9b8c` (M3 frozen base для независимого сравнения: `95354c4`).
- Ветка: `codex/m4-replay-agent`.
- Статус: `review`.
- Обновлено: `2026-09-14T08:24:12Z`.
- Scope: read-only/offline replay, синтетические fixtures и тесты эквивалентности legacy EVM/SOL учёта для AC-15..20.
- Разрешённые файлы: `tests/test_m4_replay*.py`, `tests/replay_fixtures/`, `tools/migration/replay*.py`, `docs/migration/M4_REPLAY*.md`, этот патчноут.
- Денежные production-модули, live БД, сервисы, ордера и deploy не изменяются.

## Результат

- Добавлен independent fixture oracle с frozen base `95354c4`, точным field-level diff и target SHA/dirty fingerprint.
- Known EVM/SOL fixtures требуют exact equality для position/flows/fees/funding/cost basis/PnL/state/identity.
- Crash/missing fixtures классифицируются `unsafe_legacy_fallback`: strict net, cost basis и PnL остаются unknown;
  полный CLI без `--allow-unsafe` возвращает ненулевой код.
- Проверены partial state, overlap pages, conflicting duplicates, разные native scopes, multiplier=1000,
  EVM address/Solana mint case, held reserves и idempotent notification ACK.
- Fixture/report hashes и незакрытые границы AC-15..20: `docs/migration/M4_REPLAY_REPORT.md`.

## Проверки

- Replay scope + прежние M4/HL/SOL accounting tests: `42 passed in 1.21s`.
- Только новые replay tests: `11 passed in 0.32s` на `aaf5737`.
- На checkout основного исполнителя `1a37ea8+dirty:e8a5b964fa0d`: `11 passed in 0.31s`; known PASS exact,
  unknown UNSAFE без field mismatches.
- `py_compile` пройден. Ruff в указанном venv отсутствует (`No module named ruff`).

Совместимость: runtime schema и production readers не менялись; replay пишет только disposable temp DB.
Откат: удалить новые replay/tests/docs/fixture файлы. Выкат не проводился. Push выполняет координатор после secret scan.
